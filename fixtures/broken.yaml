openapi: "3.0.0"
info:
  title: Broken API
  version: 1.0.0
paths:
  /widgets/{id}:
    get:
      operationId: getWidget
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DoesNotExist'
